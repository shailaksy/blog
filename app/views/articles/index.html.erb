<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Author</th>
            <th>Body</th>
        </tr>

    <thead>

    <tbody>
    <%  @articles.each do |article| %>
        <tr>
            <td><%= article.id %> </td>
            <td><%= article.name %> </td>
            <td><%= article.author %> </td>
            <td><%= article.body %> </td>
        </tr>
    <% end %>
    </tbody>
</table>

<%= link_to "New Article", new_article_path %>

